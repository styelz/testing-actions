name: Build and Create Artifact
run-name: ${{ github.actor }} is testing out Upload Artifact 🚀
on:
  push:
jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Create Data
        env:
          PR_NUMBER: ${{ github.event.number }}
        run: |
          mkdir -p ./data
          echo $PR_NUMBER > ./data/test.data
          ls -lR
      - uses: actions/upload-artifact@v3
        with:
          name: test_artifact
          path: data/
